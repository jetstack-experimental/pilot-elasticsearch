node.data: ${NODE_DATA}
node.master: ${NODE_MASTER}
node.ingest: ${NODE_INGEST}
node.name: ${HOSTNAME}

network.host: 0.0.0.0

cloud:
  kubernetes:
    service: ${DISCOVERY_HOST}
    namespace: ${NAMESPACE}

discovery:
  type: kubernetes

xpack.security.enabled: false
